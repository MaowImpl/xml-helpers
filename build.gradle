allprojects {
    apply plugin: 'java'

    group = 'maow'
    version = '1.0.0'
}

archivesBaseName = 'xml-helpers-all'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':xml-helpers-dom')
    implementation project(':xml-helpers-sax')
}